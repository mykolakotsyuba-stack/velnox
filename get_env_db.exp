#!/usr/bin/expect -f
set password "12345678"
spawn ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null user@mail.irbis.ua
expect "password:" { send "$password\r" }
expect "$ " { send "grep 'DB_CONNECTION' /srv/projects/velnox/api/.env\r" }
expect "$ " { send "exit\r" }
expect eof
