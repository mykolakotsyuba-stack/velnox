#!/usr/bin/expect -f

set timeout 30
spawn ssh user@mail.irbis.ua "journalctl -u velnox-frontend -n 100 --no-pager"
expect {
    "password:" {
        send "12345678\r"
        exp_continue
    }
    eof
}
